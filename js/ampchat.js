
// add event listeners
window.addEventListener('load', adjustBlocks);
window.addEventListener('resize', adjustBlocks);

// get main blocks
var mainChatWrapper = document.getElementById('MainChatWrapper');
var chatWrapper = document.getElementById('ChatWrapper');
var sideWrapper = document.getElementById('SideWrapper');

// corrects sizes of blocks
function adjustBlocks() {
//	mainChatWrapper.setAttribute('height', mainChatWrapper.parentNode.offsetHeight);

//	chatWrapper.innerHTML = mainChatWrapper.parentNode.offsetHeight;
}